# Scanning Workspaces
The primary command is `scan`. It performs deterministic AST traversal over Rust smart contract repositories.

### Usage
`stellarpath scan . --format markdown > ARCHITECTURE.md`
